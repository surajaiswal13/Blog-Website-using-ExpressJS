<%- include("partials/header"); -%>

<h1>Home</h1>

<p><%= homeContent %></p>


<% posts.forEach((element) => { %>
  <h1> <%= element.title %> </h3>
  <p> <%= element.body.substring(0, 100) + " ..." %>
    <a href="/posts/<%= element.title %>">Read More</a>
  </p>

<% }) %>

<%- include("partials/footer") -%>
